enum Color {
    RED,
    GREEN,
    BLUE, 
    YELLOW
};

enum Status {
    OK = 1,
    FAIL,
    UNKNOWN = 5,
    RETRY
};

int main() {
    Color c;
    Status s;
    int GREEN;

    c = GREEN;

    // πρέπει να βρει: GREEN είναι enum constant του Color

    // c = YELLOW;   // implicit value = 11

    // s = FAIL;     // implicit = 2
    // s = RETRY;    // implicit = 6

    return 0;
}
